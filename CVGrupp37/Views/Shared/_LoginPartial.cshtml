@using Microsoft.AspNet.Identity
@using CVGrupp37.Controllers

@if (Request.IsAuthenticated)
{
    using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm", @class = "navbar-right" }))
    {
    @Html.AntiForgeryToken()

<ul class="nav navbar-nav navbar-right">

    <li>

        @*@Html.ActionLink("Meddelanden", "Index", "Messages", routeValues: null, htmlAttributes: new { title = "Meddelande", type = "button", @class = "btn btn-primary" })*@

        <a href="@Url.Action("Index", "Messages")" title="Meddelanden">Meddelanden <span class="badge badge-primary"> @MessagesController.GetAntalMessages()</span></a>


    </li>

    

    <li>
        @Html.ActionLink("Hello " +  User.Identity.Name + "!", "Index", "Manage", routeValues: null, htmlAttributes: new { title = "Manage" })
    </li>
    <li><a href="javascript:document.getElementById('logoutForm').submit()">Log off</a></li>
</ul>
    }
}
else
{
    <ul class="nav navbar-nav navbar-right">
        <li>@Html.ActionLink("Register", "Register", "Account", routeValues: null, htmlAttributes: new { id = "registerLink" })</li>
        <li>@Html.ActionLink("Log in", "Login", "Account", routeValues: null, htmlAttributes: new { id = "loginLink" })</li>
    </ul>
}
